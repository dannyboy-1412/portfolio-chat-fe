# Portfolio (Vite)

Static Vite + React site. Read `.cursor/rules/` before changing layout, content, or routing.

Canonical content: `src/shared/profile.ts`, `src/shared/projects.ts`.
Archived assistant prompt: `src/server/ai/systemPrompt.ts` (not imported by the client).
